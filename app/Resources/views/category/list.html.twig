<h1>All products</h1>
<div>
    <table class="table">
        {% for product in pagination %}
            <tr class="info">
                <td>{{ product.name }}</td>
            </tr>
        {% endfor %}
    </table>
</div>

<div class="navigation">
    {{ knp_pagination_render(pagination) }}
</div>